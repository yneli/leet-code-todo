# Сравнение сложности алгоритмов — Наглядная таблица

## Таблица количества операций

| Элементов (n) | O(1) | O(log n) | O(n) | O(n log n) | O(n²) | O(2ⁿ) | O(n!) |
|---------------|------|----------|------|------------|-------|-------|-------|
| 1 | **1** | **1** | 1 | 1          | 1 | 2 | 1 |
| 10 | **1** | **3** | 10 | 30         | 100 | 1,024 | 3,628,800 |
| 100 | **1** | **7** | 100 | 700        | 10,000 | 1.27×10³⁰ | 9.33×10¹⁵⁷ |
| 1,000 | **1** | **10** | 1,000 | 10,000     | 1,000,000 | ∞ | ∞ |
| 10,000 | **1** | **13** | 10,000 | 130,а000   | 100,000,000 | ∞ | ∞ |
| 100,000 | **1** | **17** | 100,000 | 1,700,000  | 10,000,000,000 | ∞ | ∞ |
| 1,000,000 | **1** | **20** | 1,000,000 | 20,000,000 | 1,000,000,000,000 | ∞ | ∞ |

### Легенда:
- 🟢 **Отлично** — мгновенно
- 🟡 **Хорошо** — быстро
- 🟠 **Нормально** — приемлемо
- 🔴 **Плохо** — очень медленно
- ⚫ **Катастрофа** — непригодно

---

## Время выполнения (при 1 операции = 1 наносекунда)

| Элементов (n) | O(1) | O(log n) | O(n) | O(n log n) | O(n²) | O(2ⁿ) | O(n!) |
|---------------|------|----------|------|------------|-------|-------|-------|
| 10 | мгновенно | мгновенно | мгновенно | мгновенно | мгновенно | 1 мкс | 3.6 мс |
| 100 | мгновенно | мгновенно | мгновенно | мгновенно | 0.01 мс | века | эпохи |
| 1,000 | мгновенно | мгновенно | мгновенно | мгновенно | 1 мс | - | - |
| 10,000 | мгновенно | мгновенно | мгновенно | мгновенно | 100 мс | - | - |
| 100,000 | мгновенно | мгновенно | мгновенно | 2 мс | 10 сек | - | - |
| 1,000,000 | мгновенно | мгновенно | 1 мс | 20 мс | 17 мин | - | - |
| 10,000,000 | мгновенно | мгновенно | 10 мс | 0.2 сек | 28 часов | - | - |

---

## Реальные примеры из LeetCode

### O(1) — Константа
```javascript
// Доступ по индексу
arr[5]              // 1 операция
map.get("key")      // 1 операция
set.has(42)         // 1 операция
```

### O(log n) — Логарифмическая
```javascript
// Бинарный поиск
binarySearch([1..1000], 500)     // ~10 операций
binarySearch([1..1000000], 500000) // ~20 операций
```

### O(n) — Линейная
```javascript
// Один проход по массиву
findMax([1,2,3,4,5])              // 5 операций
findMax([1..1000])                // 1,000 операций
findMax([1..1000000])             // 1,000,000 операций
```

### O(n log n) — Линейно-логарифмическая
```javascript
// Эффективные сортировки
mergeSort([1..100])              // ~700 операций
mergeSort([1..10000])            // ~130,000 операций
quickSort([1..100000])           // ~1,700,000 операций
```

### O(n²) — Квадратичная
```javascript
// Вложенные циклы
bubbleSort([1..100])             // 10,000 операций
bubbleSort([1..1000])            // 1,000,000 операций
bubbleSort([1..10000])           // 100,000,000 операций (медленно!)
```

### O(2ⁿ) — Экспоненциальная
```javascript
// Рекурсия без мемоизации
fibonacci(10)                    // 1,024 операций
fibonacci(20)                    // 1,048,576 операций
fibonacci(30)                    // 1,073,741,824 операций (очень медленно!)
```

---

## Визуализация роста

```
Количество операций (логарифмическая шкала)

2ⁿ  │                                          ╱
    │                                        ╱
    │                                      ╱
n²  │                                  ╱
    │                              ╱
    │                          ╱
n   │                      ╱
    │                  ╱
    │              ╱
n⋅log n │          ╱
    │      ╱
log n │  ╱
O(1) ───────────────────────────────────────────
    1   10   100   1,000   10,000   100,000
                    Количество элементов (n)
```

---

## Практическое правило

| Ограничение LeetCode | Макс. n для O(n²) | Макс. n для O(n log n) | Макс. n для O(n) |
|---------------------|-------------------|------------------------|------------------|
| 1 секунда | ~5,000 | ~500,000 | ~10,000,000 |
| 2 секунды | ~7,000 | ~700,000 | ~20,000,000 |

### Когда что использовать:

**До 100 элементов:** Можно всё (O(n²), O(2ⁿ))
**До 10,000 элементов:** Избегать O(n²)
**До 1,000,000 элементов:** Нужен O(n) или O(n log n)
**Более 1,000,000:** Нужен O(n) или O(log n)

---

## Шпаргалка для интервью

```
Видишь отсортированный массив?
  → Используй бинарный поиск O(log n)

Видишь вложенные циклы?
  → Скорее всего O(n²)
  → Попробуй HashMap для O(n)

Видишь дерево/граф?
  → DFS/BFS обычно O(V + E)

Нужно найти подмассив/подстроку?
  → Sliding Window O(n)

Нужно перебрать комбинации?
  → Backtracking (может быть O(2ⁿ))
  → Но можно оптимизировать!
```

---

## Запомнить легко:

```
O(1)    → Одна операция всегда
O(log n) → Делим пополам каждый раз
O(n)    → Один проход
O(n²)   → Вложенные циклы
O(2ⁿ)   → Перебор всех вариантов (плохо!)
```

> **Главное:** O(log n) почти не растет даже с миллионами элементов! 🚀
