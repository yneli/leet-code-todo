# План повторения HashMap задач

## Следующее занятие: повторение HashMap

---

### Этап 1: Перепиши по памяти (без подглядывания)

Реши эти задачи с нуля в `script.js`:

1. **217. Contains Duplicate** — Set, одна строка
2. **242. Valid Anagram** — Map с частотой, вычитание
3. **136. Single Number** — XOR трюк (`a ^ a = 0`)
4. **383. Ransom Note** — Map с частотой, проверка остатка
5. **290. Word Pattern** — два Map (биекция)
6. **349. Intersection of Two Arrays** — Set пересечение

### Этап 2: Объясни вслух

Расскажи себе или запиши голосом:

1. **Как работает HashMap внутри?** (хеш-функция → бакет → коллизия)
2. **Почему Map.get() = O(1)?** (хеш даёт индекс напрямую)
3. **Что такое коллизия?** (два ключа → один бакет → chaining или open addressing)
4. **XOR трюк для Single Number** — почему `a ^ a = 0` и `a ^ 0 = a`?

### Этап 3: Сложные задачи (если останутся силы)

Перепиши без подглядывания:

1. **49. Group Anagrams** — сортировка как ключ, группировка
2. **347. Top K Frequent Elements** — bucket sort
3. **560. Subarray Sum Equals K** — prefix sum + Map
4. **451. Sort Characters By Frequency** — bucket sort

---

## Критерий готовности

Если можешь решить задачу без подглядывания за 5-10 минут — тема усвоена.
Если застрял — не страшно, подсмотри, но потом перепиши с нуля.

---

## Напоминалки

- [ ] Повторить XOR: `a ^ a = 0, a ^ 0 = a` (из 136. Single Number)
- [ ] Повторить prefix sum логику из 560 (sum - k = начало подмассива)
- [ ] Повторить bucket sort из 347 и 451 (корзина по частоте, без сортировки)
- [ ] Повторить Sliding Window: 3. Longest Substring Without Repeating Characters (Map + два указателя)
